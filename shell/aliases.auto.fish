alias brewup "brew update; brew doctor; brew outdated; brew upgrade; brew cleanup"

alias gp "git push"

alias sign_github_vendor "codesign -f -s \"Mark Tareshawty\""

alias ls "ls -GpF"

alias mux "tmuxinator"

alias redraw "stty sane; clear"
alias reload "redraw; source ~/.config/fish/config.fish"

alias vi "nvim"
alias vim "nvim"

alias whereami "pwd"
alias webhook-proxy "ngrok http 4567 --hostname tarebyte.ngrok.io"
